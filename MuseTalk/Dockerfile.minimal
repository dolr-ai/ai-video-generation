# Minimal test Dockerfile
FROM nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04

# Just test the base image
RUN echo "Testing base image" && ls /

# Default command
CMD ["echo", "MuseTalk minimal container ready"]